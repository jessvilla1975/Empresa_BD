
--TABLA CLIENTE
CREATE TABLE CLIENTE (
  ID INT PRIMARY KEY,
  NOMBRE VARCHAR(50),
  TELEFONO VARCHAR(15)
);


--INSERTAR DATOS CLIENTE
INSERT INTO CLIENTE (ID, NOMBRE, TELEFONO) VALUES (1, 'Juan Pérez', '555-1234');
INSERT INTO CLIENTE (ID, NOMBRE, TELEFONO) VALUES (2, 'María López', '555-5678');



--TABLA PEDIDO
CREATE TABLE PEDIDO (
    Num_pedido INT PRIMARY KEY,
    Id_Cliente INT NOT NULL,
    Estado VARCHAR(50) NOT NULL,
    Abono FLOAT NOT NULL,
    Fecha_Encargo DATE NOT NULL,
    Fecha_Entrega DATE,
    FOREIGN KEY( Id_Cliente ) REFERENCES CLIENTE(ID)
);


--INSERTAR DATOS PEDIDO

INSERT INTO PEDIDO (Num_pedido, Id_Cliente, Estado, Abono, Fecha_Encargo, Fecha_Entrega) VALUES
    (101, 1, 'En proceso', 50000, '2023-01-01', '2023-01-10'),
    (102, 2, 'Entregado', 30000, '2023-02-15', '2023-02-20');


-- Crear la tabla COLEGIO
CREATE TABLE COLEGIO (
    Id INT PRIMARY KEY,
    Nombre VARCHAR(255) NOT NULL
);

-- Insertar dos registros de ejemplo
INSERT INTO COLEGIO (Id, Nombre) VALUES
    (1, 'Colegio A'),
    (2, 'Colegio B');

-- Crear la tabla UNIFORME
CREATE TABLE UNIFORME (
    Codigo VARCHAR(50) PRIMARY KEY,
    Id_Colegio INT NOT NULL,
    Tipo VARCHAR(255) NOT NULL,
    Caracteristicas VARCHAR(255),
   FOREIGN KEY( Id_Colegio ) REFERENCES COLEGIO(Id)
);

-- Insertar dos registros de ejemplo
INSERT INTO UNIFORME (Codigo, Id_Colegio, Tipo, Caracteristicas) VALUES
    ('U1', 1, 'Diario', 'Azul-Lana-Si'),
    ('U2', 2, 'Deportivo', 'Gris-Algodón-No');


CREATE TABLE PRODUCTO_TERMINADO (
    Codigo VARCHAR(50) PRIMARY KEY,
    Num_pedido INT,
    Descripcion VARCHAR(255),
    Cantidad_Existente INT,
    Sexo VARCHAR(50),
    Precio_venta FLOAT NOT NULL,
    Talla VARCHAR(10),
    Medidas VARCHAR(255),
   FOREIGN KEY(Num_pedido ) REFERENCES PEDIDO(Num_pedido)
);

-- Insertar dos registros de ejemplo en la tabla PRODUCTO_TERMINADO
INSERT INTO PRODUCTO_TERMINADO (Codigo, Num_pedido, Descripcion, Cantidad_Existente, Sexo, Precio_venta, Talla, Medidas)
VALUES
    ('PT1', 101, 'Uniforme', 2, 'Mujer', 120000, 'M', '34-36'),
    ('PT2', 102, 'Prenda', 3, 'Hombre', 50000, 'L', '32-20');

-- Crear la tabla INVENTARIO
CREATE TABLE INVENTARIO (
    Codigo varchar NOT NULL,
    Cantidad_Existente INT,
	Descripcion VARCHAR(255) NOT NULL,
    FOREIGN KEY (Codigo) REFERENCES PRODUCTO_TERMINADO (Codigo),
    PRIMARY KEY (Codigo)
);



-- Insertar dos registros de ejemplo en la tabla INVENTARIO
INSERT INTO INVENTARIO (Codigo, Cantidad_Existente, Descripcion) VALUES
    ('PT1', 2, 'Uniforme'),
    ('PT2', 3, 'Prenda');

-- Crear la tabla PROVEEDOR
CREATE TABLE PROVEEDOR (
    Nit INT PRIMARY KEY,
    Nombre VARCHAR(25),
    Nom_contacto VARCHAR(25),
    Telefono VARCHAR(20),
    Direccion VARCHAR(25)
);

-- Insertar dos registros en la tabla PROVEEDOR
INSERT INTO PROVEEDOR (Nit, Nombre, Nom_contacto, Telefono, Direccion)
VALUES
    (1, 'Hilos S.A.', 'Contacto1', '123456789', 'Dirección1'),
    (2, 'Telitas Express', 'Contacto2', '987654321', 'Dirección2');


-- crear tabla MATERIA
CREATE TABLE materia (
    Codigo INT PRIMARY KEY,
    Tipo VARCHAR(50),
    Descripcion VARCHAR(255),
    Cantidad_Exist INT,
    Unidad_Medida VARCHAR(50)
);

-- Insertar dos registros en la tabla materia
INSERT INTO MATERIA (Codigo, Tipo, Descripcion, Cantidad_Exist, Unidad_Medida)
VALUES
    (501, 'Hilo', 'Hilos azules', '10', 'Tubos'),
    (502, 'Tela', 'Tela de rallas', '25', 'Metros');





